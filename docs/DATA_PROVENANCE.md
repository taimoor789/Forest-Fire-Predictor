# Data Provenance

This tracks what every data/model artifact in this project is, what produced
it, and — most importantly — which artifacts are **quarantined**: known
invalid, not to be reused, kept around only as an audit trail.

`data/` and `model_components/` are both gitignored (see `.gitignore`), so
this file — not git history — is the durable record of where each artifact
came from. Pair it with `artifacts/manifest.json` (`ml/manifest.py`) for the
machine-readable version: every artifact the `ml/` rebuild pipeline writes is
recorded there with a run ID, a config hash, and its upstream dependencies.

## ⚠️ Quarantined — do not reuse

These were produced by the pre-rebuild ML pipeline and are known invalid.
Superseded versions are moved to `archive/` rather than deleted (Stage 12),
so nothing can load them by accident once the rebuild's artifacts exist.

| Artifact | Why it's quarantined |
|---|---|
| `data/canada_fire_grid.csv:historical_fire` | Built from NFDB's all-time record with no date filter — includes each target year's own fires. Measured: `P(fire in 2023 \| historical_fire=0) = 0.0` exactly, across 200,020 rows. The other two columns (`lat`, `lon`) are fine; only this column is quarantined. |
| `data/fwi_backtest_2023.csv` | Produced by `forest_fire_data_preparation.ipynb` cell 6's FWI engine, which still has the pre-fix DMC/DC bugs (wrong day-length table, missing moisture→DMC conversion, DC missing its 0.5× multiplier) and flat (non-seasonal) starting codes. |
| `data/ml_features_2023.csv` | Downstream of `fwi_backtest_2023.csv` — inherits all of its formula bugs, plus the wrong danger-class thresholds and the 1.15× historical-fire multiplier baked into a `risk_prob` column. |
| `data/ml_train_featured.csv`, `data/ml_test_featured.csv` | A pure date-cut of `ml_features_2023.csv` with `actual_fire` appended. No spatial holdout (`P(historical_fire=1)` is identical to 13 decimal places between the two files — every test cell was seen in training). The `risk_prob` column is a near-perfect proxy for `historical_fire` (12 discrete values from the 1.15× multiplier) — a leak trap for anyone who reuses "all numeric columns." |
| `data/locations_to_pull.csv` | The weather-sampling frame: 2,033 cells that burned in 2023 + 800 hand-picked controls (2,166 of 2,833 already `historical_fire=1`). Case-control biased, no offset/weighting applied. |
| `data/nasa_power_weather_filtered.csv` | Wind is m/s, fed into the FWI formulas unconverted (production expects km/h) — measured mean 3.49 m/s vs. production's 19.60 km/h. Daily means used where FWI requires local-noon readings — measured RH mean 87.2% (53.5% of rows >90%) vs. production's local-noon 78.7% (34.3% >90%). Single season only (2022-11-15 to 2023-12-31). |
| `model_components/fire_risk_ml_model.pkl` | Trained on `ml_train_featured.csv` + `ml_test_featured.csv` **concatenated** (`scripts/train_final_model.py:11-15`, comment: "since the split's validation job is done") — no held-out data, no evaluation computed anywhere. Top feature importance is the leaked `historical_fire` (0.137). |
| `model_components/fire_risk_ml_features.json` | Schema for the quarantined model above — same run, same invalidity. |
| `model_components/ml_tier_thresholds.json` | File timestamp (Sep 6, 02:43) is ~10 hours older than the model it's supposedly calibrated for (Sep 6, 12:33) — verified directly. It was calibrated against a *different* model fit than the one actually shipped. |

**Not quarantined — genuinely reusable, with a caveat:**

| Artifact | Status |
|---|---|
| `data/nfdb_fires_2023.csv` | Sound: 6,847 rows, zero nulls, the ~0.8% positive-longitude rows are genuine source data-entry errors correctly filtered by the notebook. Reusable as-is for a 2023-only extract, but Stage 2 needs the *full* multi-year NFDB archive, which this file is not. |
| `data/ground_truth_2023.csv` | Spatial-join methodology (EPSG:3347, NFDB filter) is sound and leak-free. The 25km buffer attribution radius is not — see the rebuild plan's Stage 3 (17.43% of fires silently dropped, buffer doesn't tile the 0.5° grid). Reusable as a *reference* for validating the Stage 3 rebuild's output, not as a direct input. |

## Active / live production artifacts (not part of the ML rebuild)

| Artifact | Produced by | Notes |
|---|---|---|
| `data/canada_fire_grid.csv` (`lat`, `lon` columns) | `forest_fire_data_preparation.ipynb` cell 2 | The live 14,952-cell grid. Read by `collect_weather_grid_eccc.py` and `fire_risk.py`. Not mutated by the ML rebuild until Stage 12, and even then only the `historical_fire` column + a new `in_canada` column are touched. |
| `data/fwi_state.json` | `fire_risk.py`'s `save_fwi_state` | Live persisted per-cell FFMC/DMC/DC + 7-day trend history. Reference distribution for Stage 5's weather-source sanity gate and Stage 11's serving-safety gate. |
| `weather_data/*.csv` | `collect_weather_grid_eccc.py` | Live daily weather pulls (HRDPS/GDPS/HRDPA). Reference for `test_weather_adapter_units.py` and `test_replay_parity.py`. |
| `fwi_predictions.json`, `model_info.json` | `fire_risk.py`'s `main()` | Live API output. Reference distribution for the Stage 11 serving-safety gate. |

## ML rebuild artifacts (produced by `ml/`, tracked in `artifacts/manifest.json`)

Populated as each stage runs. See `artifacts/manifest.json` for the
authoritative, timestamped, hash-verified record — this table is a
human-readable index, not the source of truth.

| Artifact | Stage | Status |
|---|---|---|
| `data/grid_domain_v1.parquet` | 1 | **built** — 7,537/14,952 cells (50.4%) in Canada after a real land-mask (vs. the original audit's rough box-estimate of ~71.5% in-domain); Arctic (>=70.5N) is 29.76% of in-domain cells by count but 14.98% by area |
| `data/historical_fire_by_year.parquet` | 2 | **built** — 45,222 rows (7,537 in-domain cells × 6 target years). Full NFDB archive (448,284 valid fires, 1930–2025) attributed to nearest in-domain cell (99.98% attributed, vs. the old buffer join's 82.6%). `hist_fire_any_prior` rate ~50–52% across target years. Leak-freedom verified by `test_historical_fire_leakage.py`, including a negative control confirming it catches a simulated leak. |
| `data/ground_truth_by_year.parquet` | 3 | **built** — 22,589 (cell, date) rows across 2019–2023. Nearest-cell attribution: 99.99% of in-scope fires attributed (vs. the original 25km buffer's 82.6%), median distance 17.3km. `data/ground_truth_buffer_sensitivity.parquet` is the 30km-buffer sensitivity variant (97.7% of fires matched, 14,822 multi-attributed pairs — buffers allow one fire to match several cells, unlike Voronoi). |
| `data/labels_by_year.parquet` | 4 | **built** — 8,064,590 rows (full cross product: 7,537 in-domain cells × 214 fire-season days × 5 years, 2019–2023), zero implicit negatives. Base rates: label_w0 0.273%, label_w1 0.751%, **label_w2 1.185% (primary)**. Verified: label_w0 ⊆ label_w1 ⊆ label_w2 with zero violations, zero duplicate (cell, date, year) rows. |
| `data/reference/era5/{hourly,precip}_{year}_part{N}.grib` | 5 | **acquired** — ERA5-Land via Copernicus CDS API, 2019–2023, 20 files (5 years × 2 variable groups × 2 chunks), ~1.1GB (raw source files, not manifest-tracked). CaSPAr (the original plan, same model family as production) was unreachable for 10+ hours with no outage info, so ERA5 is the fallback — a different provider than live production, residual bias expected and measurable. Each year is split into `part0`/`part1` (≤150-day chunks): CDS rejects a single ~289-day request outright with "cost limits exceeded" even though it's well under the nominal 12,000-field limit (a 214-day request the same shape had worked earlier), so chunking was required, not just an optimization. `hourly_*`: temp/dewpoint/wind-u/wind-v/pressure at the 5 UTC hours corresponding to local noon somewhere in Canada, covering `config.REPLAY_WARMUP_DAYS`=75 days before the fire season starts (not just the season itself) so Stage 6's replay has real weather to burn DC/BUI in on before the modelling window begins. `precip_*`: total_precipitation at all 24 hourly forecast-steps of each day's accumulation run (needed to reconstruct a rolling 24h sum via `ml/weather/adapter.py`, not a single reading — `tp` resets to ~0 at each day's own 00:00 UTC and accumulates monotonically through step 24, confirmed directly against real data). The CDS request uses explicit `date` strings rather than separate year/month/day lists — the latter get cross-producted independently by CDS and had silently expanded an earlier request to ~all of March–October instead of the intended range. **Chunk-boundary bug found and fixed:** CDS auto-prepends one boundary day to whichever chunk starts mid-accumulation (to supply that chunk's first hour); after concatenation that date appears in both chunks but each contributes only a partial reading (one has a valid `step=16` and NaN `step=24`, the other the reverse) — a plain last-write-wins merge silently picked the NaN half at 2 of 289 dates per cell per year. Fixed with a NaN-aware groupby-mean merge (`ml/weather/adapter.py::_dedupe_prefer_valid`); verified directly against the June 13–14, 2023 boundary before and after the fix. |
| `data/fwi_replay_{year}.parquet` | 6 | **built** — `ml/replay_fwi.py`; `test_replay_parity.py` passing (verifies the replay's `bootstrap_from_window`/`advance_one_day`/gap-reinit orchestration reduces to exactly the same result as calling `fire_risk.py` directly, using real `weather_data/*.csv` rows). |
| `data/dataset_full.parquet` | 7 | **built** — 7,342,340 rows × 27 columns (8,064,590 Stage 4 label rows minus 722,250 / 9.0% for the 675 ERA5-uncovered cells, see the Stage 5 row above). Zero NaN, zero duplicate (cell_id, date) rows. `label_w2` (primary) base rate 1.271% overall, 0.95%–1.61% by year (2023 — the real record season — highest, as expected). FFMC mean 64.06 / DMC mean 9.28 closely track live production's 64.7 / 9.14 (vs. the quarantined training data's physically-impossible FFMC mean 35.96 and DMC pinned at 500 for 54.2% of rows). `hist_fire_any_prior` rate 54.6%, consistent with Stage 2's 50–52%. |
| Stage 8 splits (`ml/splits.py`, no artifact file — computed on demand from `block_id`) | 8 | **built** — 3 pre-registered regimes (temporal, spatial group-k-fold, spatio-temporal). Spatial folds use KMeans on block centroids for geographically contiguous regions, not a random per-block assignment — a random assignment was tried first and rejected: on this project's dense 4°-block grid (172 blocks), a random test set has a same-or-adjacent-fold neighbor almost everywhere, so `SPATIAL_BLOCK_BUFFER_RING` ended up excluding 63% of all rows from training (verified directly); contiguous regions cut that to 8%. Spatio-temporal (fold 0 + `HOLDOUT_YEAR`) is pre-registered as Stage 11's deciding regime: train=4,006,936 / test=349,462 rows. Calibration split (fold 1, training years only) disjoint from both: fit_train=3,099,576 / calib=907,360. `test_splits_disjoint.py` verifies zero cell/block/date leakage across every regime, including the buffer-ring blocks themselves being excluded from train, not silently included. **Degenerate-fold bug found (during Stage 10) and fixed:** the original KMeans partition landed fold 1 (33 blocks) entirely in the high-Arctic tundra — 907,360 rows, **zero** fire labels across all 5 years, a real geographic possibility a purely spatial clustering doesn't guard against. It silently collapsed Stage 10's `IsotonicRegression` calibrator to a constant (test PR-AUC fell to exactly the base rate, 0.0337 — the signature of a non-discriminative predictor). Fixed with `spatial_folds`'s new `block_positive_counts` repair: any fold with fewer than `min_fold_positives` total positive rows has its blocks reassigned to their nearest positive-containing fold by centroid distance, purely label-count-based (never touches model performance, so it can't be used to p-hack results). **Verified fold 0 (used by every already-committed Stage 9 result) is byte-identical before and after the fix** — Stage 9's numbers did not need to be rerun. Folds collapsed from 5 to 4 after the repair. `test_no_degenerate_fold` added as a permanent regression guard. |
| `results/ablation_{run_id}.json` | 9 | **built** — B0–B4 baselines + A1–A5 ablation rungs, all 3 split regimes, block-bootstrap 95% CIs. **Deciding regime (spatio-temporal): A5 PR-AUC 0.194 vs B1 (raw FWI) 0.071 vs B2 (`get_danger_class`, live incumbent) 0.063** — 172.6% relative lift over B1 (paired CI 0.094–0.148, gate needs ≥25%), A5 beats B2 on both PR-AUC and precision@top-5% (0.228 vs 0.096). SHAP at A5: only 34.9% of \|SHAP\| is FWI-derived, 65.1% other — the lift is NOT primarily "a smarter function of FWI" (A1→A2 lift is modest, ~23% relative over B1) but overwhelmingly `historical_fire` (A3→A4 jump from 0.082 to 0.202) — matches the literature-based hypothesis that motivated this rebuild (FWI models fire behavior potential given ignition, not ignition probability itself). Gate conditions 1/2/4 (see `docs/PREREGISTRATION.md`) pass clearly; condition 3 (ECE ≤ 0.02) is **not yet evaluable** — A5's raw `predict_proba` (ECE 0.342) is expected to be badly miscalibrated since `class_weight="balanced"` deliberately distorts probabilities during training; real calibration is Stage 10's job, done on the disjoint calibration split, never on this ablation's raw output. Mandatory serving-safety distribution gate also still pending. |
| `model_components/model.pkl` + `calibrator.pkl` + `features.json` + `tiers.json` | 10 | **built** — model selected via inner CV (folds remaining after test/calibration, never touching either): `rf_original` (the unmodified hyperparameters from the quarantined `scripts/train_final_model.py`) beat a 3-config LightGBM grid, mean inner-CV PR-AUC 0.0658 vs 0.027–0.033. Fit on `fit_train` only (3,099,576 rows, calibration fold excluded), calibrated via `IsotonicRegression` on the disjoint calibration split, tier bounds derived on that same calibration split, evaluated once on the untouched test split. **Test-split results: PR-AUC 0.179 (calibrated score), ECE 0.00995 — passes the ≤0.02 gate — tier fire rates [0.0001, 0.0006, 0.0072, 0.0872], strictly monotone out-of-sample.** Only 4 distinct tiers were derivable from the calibration split's quantiles (not the 6 `get_danger_class` uses) — the calibrated-probability distribution is heavily right-skewed with many tied near-zero values, collapsing several of the 6 requested quantile boundaries; a real, honest limitation of equal-frequency binning on this label's base rate, not yet resolved (Stage 12 would need to either accept 4 tiers or try a different tier-derivation rule). All four gate conditions independently re-verified against this exact artifact in Stage 11 (see below) — PASS. |
| `results/serving_safety_check.json` (`ml/serving_safety_check.py`) | 11 | **built, PASS** — the mandatory, accuracy-independent gate: every checked feature's live production median (from `fwi_predictions.json` + `data/fwi_state.json`) falls inside `fit_train`'s 5th–95th percentile range. FFMC/DMC/DC/ISI/BUI/FWI/dc_trend_7d/bui_trend_7d all pass — e.g. DMC's training range `[0.000, 21.548]` comfortably contains the live median `5.261`, the exact check that would have instantly caught the original quarantined model's training-DMC-median-pinned-at-500 bug. `hist_fire_any_prior` checked only against an approximate live proxy (`historical_fire_zone`, itself derived from the still-quarantined flat `historical_fire` column). **`hist_fire_count_prior_20y_log1p` and `years_since_last_fire` are NOT YET CHECKABLE** — `data/canada_fire_grid.csv` doesn't carry Stage 2's leak-free count/recency features yet, only the old quarantined binary column; Stage 12 would need to regenerate that file first. Reported explicitly as a gap, not silently skipped. |
| `results/stage11_gate_decision.json` (`ml/stage11_gate.py`) | 11 | **built — SHIP.** All 4 pre-registered conditions + the serving-safety gate re-verified fresh against the exact shipping artifact (not transferred from Stage 9's ablation rung, which trained on slightly more data). Beats B1 by 151.7% relative PR-AUC (CI 0.083–0.135, gate ≥25%); beats B2 on PR-AUC (0.179 vs 0.063) and precision@top-5% (0.216 vs 0.096); ECE 0.00995 (≤0.02) with monotone tiers; A1→A2 (nonlinear FWI reshaping alone) accounts for only 11.1% of the total lift, `historical_fire` dominates. Decision and full numbers appended to `docs/PREREGISTRATION.md`'s Amendments section (2026-09-11), never edited into the original pre-registered text. **Proceed to Stage 12.** |
