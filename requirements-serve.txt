# Render's build list -- deliberately not requirements.txt.
#
# main.py imports only fastapi, pydantic, and joblib (plus stdlib): it
# never touches pandas/numpy/scikit-learn/scipy/eccodes, because it only
# ever reads pre-computed fwi_predictions.json / model_info.json off disk.
# The daily pipeline (weather collection + FWI calculation, which DOES need
# the heavy stack) runs in GitHub Actions, not on Render -- see
# .github/workflows/daily-pipeline.yml and requirements.txt.
#
# Splitting this out cuts Render's build from ~4min to ~30s, which matters
# against the free tier's 500 build-minutes/month for a daily auto-deploy.

fastapi==0.104.1
uvicorn[standard]==0.24.0
pydantic==2.11.7
joblib==1.3.2
